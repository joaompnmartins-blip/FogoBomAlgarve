version: "3.9"
services:
  db:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_DB: firedb
      POSTGRES_USER: fireuser
      POSTGRES_PASSWORD: firepass
    ports:
      - "5432:5432"

  app:
    build: .
    ports:
      - "7860:7860"
    depends_on:
      - db
